name: "CodeQL Security Scanning"

on:
  push:
    branches: [ master, main, claude/* ]
  pull_request:
    branches: [ master, main ]
  schedule:
    # Run weekly on Mondays at 6:00 UTC to catch new vulnerabilities
    - cron: '0 6 * * 1'

jobs:
  analyze:
    name: Analyze Code (${{ matrix.language }})
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: [ 'c-cpp', 'python' ]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Initialize CodeQL
      uses: github/codeql-action/init@v3
      with:
        languages: ${{ matrix.language }}
        # Build mode: autobuild automatically detects CMake for C/C++
        # Python is interpreted and doesn't require building

    - name: Autobuild
      uses: github/codeql-action/autobuild@v3

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v3
      with:
        category: "/language:${{matrix.language}}"
